# Public backend origin embedded into the browser bundle at build time.
# In Vercel set this to your deployed backend URL, e.g. https://finsight-api.example.com
NEXT_PUBLIC_API_URL=http://localhost:8000
